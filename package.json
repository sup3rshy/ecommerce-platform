{
  "name": "ecommerce-platform",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,seller,pay -c blue,green,magenta \"npm --prefix web-app run dev\" \"npm --prefix seller-workspace run dev\" \"npm --prefix shoppay run dev\"",
    "db:push": "cd web-app && npx drizzle-kit push && cd ../seller-workspace && npm run db:push && cd ../shoppay && npm run db:push"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
